# Local secrets — never commit. Real values live in Netlify's environment variables.
.env
.env.*
!.env.example

# Local Netlify folder
.netlify

# Written by `netlify dev` when it sets up the Edge Functions environment.
deno.lock
